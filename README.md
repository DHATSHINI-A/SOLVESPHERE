# SOLVESPHERE
An AI-powered platform that understands societal problems and matches them with the right partners.

### Backend Setup
1. Install dependencies:
   pip install -r requirements.txt

2. Run the workspace server:
   py -m uvicorn workspace:app --reload --port 8000
